<%= form_for @renter, :remote => true, :validate => true do |f| %>

  <%= f.text_field :postcode, :id=>"inputIcon", :placeholder=>"Postcode"%>
  <%= f.text_field :email, :id=>"inputIcon", :placeholder=>"Email"%>
  <%= f.submit "Download Your Guide", :class => 'btn' %>

<% end %>
